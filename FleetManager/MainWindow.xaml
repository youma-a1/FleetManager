<Window x:Class="FleetManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Connexion Client" Height="420" Width="420">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Champs de connexion -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Email:" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="0" Grid.Column="1" Width="220" Margin="0,5"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Mot de passe:" VerticalAlignment="Center" Margin="0,5"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Width="220" Margin="0,5"/>

        <!-- Boutons -->
        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button x:Name="LoginButton" Content="Se connecter" Width="120" Click="LoginButton_Click" Margin="6"/>
        </StackPanel>

        <!-- 🔹 NOUVEAU BOUTON DE RÉINITIALISATION -->
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0">
            <Button x:Name="ResetPasswordButton" 
                    Content="🔧 Réinitialiser mot de passe" 
                    Width="200" 
                    Click="ResetPasswordButton_Click" 
                    Background="Orange"
                    Foreground="White"
                    Margin="6"/>
        </StackPanel>

        <!-- Message de résultat -->
        <TextBlock x:Name="ResultTextBlock" Grid.Row="4" Grid.ColumnSpan="2" Text="" Foreground="Red" Margin="0,8,0,0" TextAlignment="Center" TextWrapping="Wrap"/>

        <!-- Zone image boutique -->
        <Border Grid.Row="5" Grid.ColumnSpan="2" BorderBrush="LightGray" BorderThickness="1" Padding="8" CornerRadius="6" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0">
            <StackPanel HorizontalAlignment="Center">
                <Image x:Name="BoutiqueImage" Width="120" Height="80" Visibility="Collapsed" Stretch="Uniform" MouseLeftButtonUp="BoutiqueImage_MouseLeftButtonUp" Cursor="Hand"/>
                <TextBlock x:Name="BoutiqueHint" Text="(Choisis une image après connexion)" Visibility="Collapsed" FontSize="11" HorizontalAlignment="Center" Margin="4"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>